(function () {
    'use strict';
    var App = angular.module('app');
    App.controller('feeCollectionCtrl', feeCollectionController);
    feeCollectionController.$inject = ['$scope', '$localStorage', '$window', '$http'];

    function feeCollectionController($scope, $localStorage, $window, $http) {
    
    }
})();