(function () {
    'use strict';
    var App = angular.module('app');
    App.controller('concessionRulesCtrl', concessionRulesController);
    concessionRulesController.$inject = ['$scope', '$localStorage', '$window', '$http'];

    function concessionRulesController($scope, $localStorage, $window, $http) {
    
    }
})();